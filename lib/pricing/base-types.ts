import type { BaseType } from './engine'

/** Canonical display order for base-type UI (radio picker, etc). Labels/descriptions live in the i18n dictionaries. */
export const BASE_TYPE_VALUES: BaseType[] = ['none', 'acrylic_clear', 'acrylic_black', 'led']
